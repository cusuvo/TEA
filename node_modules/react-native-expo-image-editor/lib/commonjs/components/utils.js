"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getImageLayout = getImageLayout;
exports.getImageSize = getImageSize;
exports.height = void 0;
exports.resizeImage = resizeImage;
exports.width = void 0;

var _reactNative = require("react-native");

// src/components/utils.ts
const {
  width,
  height
} = _reactNative.Dimensions.get('window');

exports.height = height;
exports.width = width;

function resizeImage(imageWidth, imageHeight, screenWidth, screenHeight) {
  const ratio = Math.min(screenWidth / imageWidth, screenHeight / imageHeight);
  return {
    width: imageWidth * ratio,
    height: imageHeight * ratio
  };
}

function getImageSize(uri) {
  return new Promise((resolve, reject) => {
    _reactNative.Image.getSize(uri, (actualWidth, actualHeight) => resolve({
      width: actualWidth,
      height: actualHeight
    }), error => reject(error));
  });
}

async function getImageLayout(uri, previewWidth, previewHeight) {
  const layout = await getImageSize(uri);
  const resize = resizeImage(layout.width, layout.height, previewWidth, previewHeight);
  return resize;
}
//# sourceMappingURL=utils.js.map