// rotateImage.ts
import { Image } from 'react-native';
import * as ImageManipulator from 'expo-image-manipulator';

async function rotate(uri, // width: number,
// height: number,
saveOptions) {
  return ImageManipulator.manipulateAsync(uri, [{
    rotate: -90
  }], saveOptions);
}

export default function rotateImage(uri, saveOptions) {
  return new Promise((resolve, reject) => {
    Image.getSize(uri, async (width, height) => {
      const {
        uri: newUri,
        base64
      } = await rotate(uri, // width,
      // height,
      saveOptions);
      resolve({
        size: {
          width: height,
          height: width
        },
        uri: newUri,
        base64
      });
    }, errors => reject(errors));
  });
}
//# sourceMappingURL=rotateImage.js.map