// Gird.tsx
import React from 'react';
import GridEdge from './GridEdge';
import { grids } from './constants';
import GridRow from './GridRow';
import GridCol from './GridCol';

function Grid() {
  const renderRow = (row, rowIndex) => {
    return row.map((col, colIndex) => {
      if (col === undefined) return /*#__PURE__*/React.createElement(GridCol, {
        key: `${rowIndex}-${colIndex}`
      });
      const {
        justifyContent,
        alignSelf,
        top,
        left,
        bottom,
        right
      } = col;
      return /*#__PURE__*/React.createElement(GridCol, {
        key: `${rowIndex}-${colIndex}`,
        style: {
          justifyContent
        }
      }, /*#__PURE__*/React.createElement(GridEdge, {
        top,
        left,
        bottom,
        right,
        alignSelf
      }));
    });
  };

  return /*#__PURE__*/React.createElement(React.Fragment, null, grids.map((row, rowIndex) => {
    return /*#__PURE__*/React.createElement(GridRow, {
      key: `r-${rowIndex}`
    }, renderRow(row, rowIndex));
  }));
}

export default /*#__PURE__*/React.memo(Grid);
//# sourceMappingURL=Grid.js.map