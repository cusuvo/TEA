// src/components/Provider.tsx
import React from 'react';
const initialState = {
  imageUri: '',
  setImageUri: () => {},
  manipulator: {
    visible: false,
    label: 'Rotate',
    setVisible: () => {}
  }
};
export const EditorContext = /*#__PURE__*/React.createContext(initialState);
export const Provider = props => {
  const {
    uri,
    children
  } = props;
  const [imageUri, setImageUri] = React.useState(uri);
  const [visible, setVisible] = React.useState(false);
  return /*#__PURE__*/React.createElement(EditorContext.Provider, {
    value: {
      imageUri,
      setImageUri,
      manipulator: {
        visible,
        setVisible,
        label: ''
      }
    }
  }, children);
};
export const Consumer = EditorContext.Consumer;
export const useProvider = () => React.useContext(EditorContext);
//# sourceMappingURL=Provider.js.map