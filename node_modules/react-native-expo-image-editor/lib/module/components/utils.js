// src/components/utils.ts
import { Image, Dimensions } from 'react-native';
export const {
  width,
  height
} = Dimensions.get('window');
export function resizeImage(imageWidth, imageHeight, screenWidth, screenHeight) {
  const ratio = Math.min(screenWidth / imageWidth, screenHeight / imageHeight);
  return {
    width: imageWidth * ratio,
    height: imageHeight * ratio
  };
}
export function getImageSize(uri) {
  return new Promise((resolve, reject) => {
    Image.getSize(uri, (actualWidth, actualHeight) => resolve({
      width: actualWidth,
      height: actualHeight
    }), error => reject(error));
  });
}
export async function getImageLayout(uri, previewWidth, previewHeight) {
  const layout = await getImageSize(uri);
  const resize = resizeImage(layout.width, layout.height, previewWidth, previewHeight);
  return resize;
}
//# sourceMappingURL=utils.js.map