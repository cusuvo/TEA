import * as ImageManipulator from 'expo-image-manipulator';
declare type rotatedImage = {
    uri: string;
    base64: string | undefined;
    size: {
        width: number;
        height: number;
    };
};
export default function rotateImage(uri: string, saveOptions: ImageManipulator.SaveOptions): Promise<rotatedImage>;
export {};
