import * as ImageManipulator from 'expo-image-manipulator';
import type { Size } from '../types/cropImage';
declare type CroppedImageType = {
    actualSize: Size;
    uri: string;
    base64: string | undefined;
};
export default function cropImage(uri: string, bounding: any, saveOptions: ImageManipulator.SaveOptions): Promise<CroppedImageType>;
export {};
