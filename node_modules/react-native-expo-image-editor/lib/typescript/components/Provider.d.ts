import React from 'react';
interface IState {
    imageUri: string;
    setImageUri: React.Dispatch<any>;
    manipulator: {
        visible: boolean;
        label: string;
        setVisible: React.Dispatch<any>;
    };
}
interface IProviderProps {
    children: React.ReactNode;
    uri: string;
}
export declare const EditorContext: React.Context<IState>;
export declare const Provider: (props: IProviderProps) => JSX.Element;
export declare const Consumer: React.Consumer<IState>;
export declare const useProvider: () => IState;
export {};
