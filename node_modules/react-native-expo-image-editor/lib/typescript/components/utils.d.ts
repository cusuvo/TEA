export declare const width: number, height: number;
export declare function resizeImage(imageWidth: number, imageHeight: number, screenWidth: number, screenHeight: number): {
    width: number;
    height: number;
};
export declare function getImageSize(uri: string): Promise<{
    width: number;
    height: number;
}>;
export declare function getImageLayout(uri: string, previewWidth: number, previewHeight: number): Promise<{
    width: number;
    height: number;
}>;
