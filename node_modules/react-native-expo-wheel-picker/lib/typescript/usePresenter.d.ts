interface IProps {
    initialValue: any | undefined;
    items: any[];
    valueAttribute: string;
    labelAttribute: string;
    numberOfVisibleRows: number;
}
export default function usePresenter({ initialValue, items: propItems, valueAttribute, labelAttribute, numberOfVisibleRows, }: IProps): {
    items: {
        value: any;
        label: any;
    }[];
    onValueChange: (event: any) => void;
    currentIndex: number;
    defaultIndex: number;
    getRowItemByIndex: (index: number) => {
        value: any;
        label: any;
    };
    height: number;
};
export {};
